// create an array of words (driver code)
//break each word into a seperate values in array
//measure the length of each value. convert string to integer
//convert the value to the integer equivelant in length (so value = value.length)
// return the longest value length as its original value (as a string)
//end 


function word_length(str){ 
		var 
}

